{
  "name": "matts-second-brain",
  "version": "0.1.0",
  "description": "A persistent knowledge brain for Claude Code, backed by Trilium Notes. Standards, decisions, lessons learned, and per-app context that survive across sessions and grow over time. Includes a setup wizard, an MCP server with brain_recall / brain_remember / brain_scan_transcripts tools, and a discipline skill that makes Claude actually use it.",
  "author": {
    "name": "Matt Hesketh",
    "email": "matt@heskethwebdesign.co.uk"
  },
  "license": "MIT",
  "keywords": [
    "memory",
    "knowledge-base",
    "trilium",
    "second-brain",
    "claude-code",
    "mcp",
    "skills",
    "self-improvement"
  ],
  "userConfig": {
    "sessionReminder": {
      "type": "boolean",
      "default": true,
      "description": "Inject the brain discipline at session start (knowledge-only, recall-before, capture-after). Set false to rely on the auto-loaded skill alone."
    },
    "scheduledMaintenance": {
      "type": "boolean",
      "default": false,
      "description": "Run unattended nightly brain maintenance (scan transcripts, persist durable knowledge, reindex). Enable, then run /matts-second-brain:maintenance install."
    },
    "maintenanceSchedule": {
      "type": "string",
      "default": "0 4 * * *",
      "description": "Cron expression (UTC) for scheduled maintenance."
    },
    "maintenanceModel": {
      "type": "string",
      "default": "claude-sonnet-4-6",
      "description": "Model the unattended maintenance run uses."
    },
    "scanSinceDays": {
      "type": "number",
      "default": 2,
      "description": "How many days of recent transcripts the maintenance scan considers."
    },
    "maintenanceTimeoutMin": {
      "type": "number",
      "default": 10,
      "description": "Hard timeout (minutes) for the unattended maintenance agent run."
    }
  }
}
